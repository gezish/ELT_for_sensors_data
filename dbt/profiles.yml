postgres_new:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: airflow
      pass: airflow
      port: 5432
      dbname: pneuma
      schema: public
      threads: 4